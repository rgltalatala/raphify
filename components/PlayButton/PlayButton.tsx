import { FaPlay } from "react-icons/fa";

import styles from "./PlayButton.module.scss";

const PlayButton = () => {
  return <FaPlay className={styles.playButtonIcon} />;
};

export default PlayButton;
